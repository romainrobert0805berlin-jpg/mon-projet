import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'fr.claubert.app',
  appName: 'Claubert',
  webDir: 'dist',
  backgroundColor: '#fbf7f0',
}

export default config
